# git init
# git add . 
# git commit - "your message goes here"
# git status
# git log (hit 'q' to exit)
# git checkout (unique commit id)

Git remote commands...
# git remote add origin (url)
# git remote -v (to check whether there is a remote connection)
# git push origin master
# git pull origin master